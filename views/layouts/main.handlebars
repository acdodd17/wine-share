<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wine Share</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Meddon&family=Tenali+Ramakrishna&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="container-fluid">
    <div class="row header">
      <div class="col title fs-1 m-4 text-center d-flex align-items-center justify-content-start">
        <a href="/">Wine Share</a> 
      </div>
      <div class="col m-4 ps-5 fs-4 d-flex align-items-center justify-content-end">
        {{#if loggedIn}}
        <div id="dash" class="mx-2"><a href="/dashboard" >Dashboard</a></div>
        <div id="logout" class="mx-2"><button id="logout">Logout</button></div>
        {{else}}
        <div id="login" class="mx-2"><a href="/login">Login/Sign-up</a></div>
        {{/if}}
      </div>
    </div>
  </div>
  <div class="container-fluid">
    <main>
      {{{ body }}}
    </main>
  </div> 
  <div class="push"></div>
  <div class="container-fluid">
    <footer class="footer text-center mt-2 p-2 fs-3">
       &copy; Wine Share Inc. 🍷
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>